# Rust／桌面標準專案包模板

```text
manager: Cargo
lockfile: Cargo.lock
toolchain: rust-toolchain.toml or rust-version
frontend JS (if any): follow JavaScript package rules separately
commands (PROJECT.md): install / dev / test / lint / build / clean
```
