# Python 標準專案包模板

```text
manager: uv
lockfile: uv.lock
manifest: pyproject.toml
runtime: .python-version
venv: .venv (project-owned)
commands (PROJECT.md): install / dev / test / lint / build / clean
```
