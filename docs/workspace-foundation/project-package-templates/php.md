# PHP／WordPress 標準專案包模板

```text
manager: Composer
lockfile: composer.lock
manifest: composer.json
deps: vendor/ (project-owned)
commands (PROJECT.md): install / dev / test / lint / build / clean
```
